# 访问监控与深呼吸引导功能

## 功能概述

这个功能为Chrome扩展添加了智能访问监控系统，当用户过度访问被阻止的网站时，会自动引导用户进入深呼吸页面，帮助用户重新获得专注力。

## 主要特性

### 1. 访问监控系统
- **24小时滚动窗口监控**：跟踪用户对被阻止网站的访问行为
- **访问次数统计**：记录每个网站的访问次数
- **停留时间记录**：精确记录用户在每个网站的停留时间
- **智能阈值检测**：当超过设定阈值时自动触发干预

### 2. 深呼吸引导页面
- **呼吸引导动画**：提供视觉化的呼吸节奏指导
- **摄像头监控**：可选启用摄像头来监控用户状态
- **自定义提醒消息**：支持用户自定义的鼓励性文案
- **渐进式体验**：优雅的动画和交互设计

### 3. 用户配置选项
- **阈值设置**：可自定义访问次数和时间限制
- **消息定制**：个性化的提醒文案
- **实时统计**：查看24小时内的访问数据
- **数据管理**：支持数据清理和重置

## 技术实现

### 存储系统
- 新增 `visitMonitorStorage` 存储模块
- 支持24小时滚动窗口数据管理
- 自动清理过期数据

### 监控机制
- 集成到现有的 `UrlBlocker` 系统
- 在标签页事件中记录访问时长
- 实时阈值检测和页面跳转

### 用户界面
- 在侧边栏添加"访问监控"配置页面
- 深呼吸页面支持摄像头权限请求
- 响应式设计，支持深色/浅色主题

## 使用方法

### 1. 配置阈值
1. 打开Chrome扩展侧边栏
2. 切换到"访问监控"标签
3. 设置24小时内最大访问次数（默认5次）
4. 设置24小时内最大停留时间（默认15分钟）
5. 点击"保存阈值设置"

### 2. 自定义消息
1. 在"自定义提醒消息"区域输入个性化文案
2. 点击"保存消息"
3. 该消息将在深呼吸页面显示

### 3. 查看统计
- 在"24小时访问统计"区域查看实时数据
- 包括访问次数、停留时间和进度条
- 支持手动刷新数据

### 4. 深呼吸体验
当触发阈值时：
1. 自动跳转到深呼吸页面
2. 查看触发原因和网站信息
3. 可选启用摄像头监控
4. 跟随呼吸引导进行5分钟深呼吸
5. 完成后可返回或跳过（10秒后可用）

## 文件结构

```
packages/storage/lib/impl/
├── visitMonitorStorage.ts          # 访问监控存储模块

chrome-extension/src/background/managers/
├── urlBlocker.ts                   # 集成访问监控逻辑

chrome-extension/public/
├── deep-breathing.html             # 深呼吸页面
├── deep-breathing.js               # 深呼吸页面逻辑

pages/side-panel/src/components/
├── VisitMonitorSettings.tsx        # 访问监控设置组件
```

## 配置选项

### 默认设置
- 最大访问次数：5次/24小时
- 最大停留时间：15分钟/24小时
- 深呼吸时长：5分钟
- 跳过按钮延迟：10秒

### 可自定义项
- 访问次数阈值（1-50次）
- 停留时间阈值（1-120分钟）
- 自定义提醒消息
- 是否启用监控功能

## 数据管理

### 自动清理
- 每小时自动清理24小时前的数据
- 启动时执行一次清理任务

### 手动操作
- **清理旧数据**：删除24小时前的访问记录
- **重置统计**：清空所有访问记录和统计数据

## 隐私保护

- 所有数据仅存储在本地Chrome存储中
- 摄像头权限完全由用户控制
- 不会上传任何访问数据到服务器
- 支持完全禁用监控功能

## 兼容性

- 兼容现有的网站阻止功能
- 不影响学习模式的正常运行
- 支持所有现有的主题和设置

## 故障排除

### 常见问题
1. **阈值不生效**：检查是否启用了访问监控功能
2. **摄像头无法启用**：检查浏览器摄像头权限设置
3. **数据不准确**：尝试清理旧数据并重新开始监控

### 调试信息
- 在浏览器控制台查看 `[UrlBlocker]` 相关日志
- 检查Chrome存储中的 `visit-monitor-storage-key` 数据

## 未来改进

- 添加更多呼吸引导模式
- 支持自定义深呼吸时长
- 添加访问趋势图表
- 集成更多放松技巧
