# 访问监控功能测试指南

## 🔧 修复内容

已修复访问监控功能，现在插件安装后会立即开始记录所有被监控网站的访问，无需开启专注模式。

### 主要修复点：

1. **独立监控**：访问记录与专注模式解耦，插件安装后即开始监控
2. **智能记录**：避免重复记录，优化访问时长计算
3. **完整跟踪**：监听标签页关闭、URL变化等事件，确保访问时长准确
4. **实时统计**：立即更新访问次数和停留时间

## 🧪 测试步骤

### 1. 准备测试环境

1. **加载扩展**：
   ```bash
   # 在Chrome中加载 dist 目录作为未打包扩展
   chrome://extensions/ -> 开发者模式 -> 加载已解压的扩展程序
   ```

2. **配置监控网站**：
   - 打开侧边栏（右键扩展图标 -> 侧边栏）
   - 在"网站配置"中添加测试网站（如 xiaohongshu.com）
   - 或者在"全局设置"中添加到阻止列表

3. **配置监控阈值**：
   - 切换到"访问监控"标签
   - 设置较低的阈值便于测试（如：2次访问，1分钟停留）
   - 启用访问监控功能

### 2. 测试访问记录

#### 测试场景1：基础访问记录
1. **不开启专注模式**
2. 访问 xiaohongshu.com（或您配置的测试网站）
3. 停留10-20秒后关闭标签页
4. 重复访问2-3次

**预期结果**：
- 在侧边栏"访问监控"页面能看到访问记录
- 访问次数正确累计
- 停留时间被记录

#### 测试场景2：阈值触发测试
1. 继续访问测试网站，直到超过设定阈值
2. 观察是否自动跳转到深呼吸页面

**预期结果**：
- 超过阈值时自动跳转到 `deep-breathing.html`
- 页面显示触发原因和网站信息
- 可以进行深呼吸练习或跳过

#### 测试场景3：专注模式集成
1. 开启专注模式
2. 访问被阻止的网站
3. 观察是否根据阈值显示不同页面

**预期结果**：
- 未超过阈值：显示普通 `blocked.html`
- 超过阈值：显示 `deep-breathing.html`

### 3. 验证数据准确性

#### 检查访问统计
1. 打开侧边栏"访问监控"页面
2. 查看"24小时访问统计"区域
3. 验证数据准确性：
   - 访问次数是否正确
   - 停留时间是否合理
   - 进度条是否正确显示

#### 检查控制台日志
1. 打开Chrome开发者工具
2. 查看Console标签
3. 寻找以下日志：
   ```
   UrlBlocker: Monitored URL detected, recording visit: [URL]
   VisitMonitor: Added new record for [domain] duration: [time]s
   UrlBlocker: Updated visit duration: [URL] [time]s
   ```

### 4. 测试边界情况

#### 快速切换标签页
1. 快速打开和关闭监控网站
2. 验证是否正确记录短时间访问

#### URL变化
1. 在同一标签页内从监控网站跳转到其他网站
2. 验证是否正确记录访问时长

#### 标签页关闭
1. 直接关闭包含监控网站的标签页
2. 验证是否记录最终访问时长

## 🐛 故障排除

### 常见问题

1. **访问次数不增加**：
   - 检查网站是否在监控列表中
   - 查看控制台是否有错误日志
   - 确认访问监控功能已启用

2. **停留时间不准确**：
   - 短时间访问（<5秒）可能被合并
   - 检查标签页是否正确关闭
   - 查看控制台日志确认时长计算

3. **阈值不触发**：
   - 确认阈值设置正确
   - 检查是否在专注模式下测试
   - 验证网站域名匹配

### 调试信息

#### 查看存储数据
```javascript
// 在控制台执行
chrome.storage.local.get(['visit-monitor-storage-key'], (result) => {
  console.log('Visit Monitor Data:', result);
});
```

#### 手动清理数据
```javascript
// 重置访问统计
chrome.storage.local.get(['visit-monitor-storage-key'], (result) => {
  const config = result['visit-monitor-storage-key'];
  config.records = [];
  config.stats = {};
  chrome.storage.local.set({'visit-monitor-storage-key': config});
});
```

## ✅ 验证清单

- [ ] 插件安装后立即开始记录访问（无需专注模式）
- [ ] 访问次数正确累计
- [ ] 停留时间准确记录
- [ ] 阈值触发正常工作
- [ ] 深呼吸页面正确显示
- [ ] 统计数据实时更新
- [ ] 标签页关闭时正确记录时长
- [ ] URL变化时正确处理
- [ ] 控制台日志正常输出

## 📝 测试报告模板

```
测试环境：Chrome版本 [版本号]
测试时间：[日期时间]
测试网站：[测试的网站列表]

测试结果：
1. 基础访问记录：✅/❌
2. 阈值触发：✅/❌
3. 数据准确性：✅/❌
4. 边界情况：✅/❌

发现问题：
[描述任何发现的问题]

建议改进：
[提出改进建议]
```

现在您可以按照这个指南测试修复后的访问监控功能！
