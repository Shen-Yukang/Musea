# DOM 配置系统使用指南

## 概述

新的DOM配置系统允许用户自定义网站的DOM元素操作，替代了之前硬编码的网站处理逻辑。用户可以通过Side Panel界面配置各种DOM操作，包括隐藏、模糊、淡化、动画等效果。

## 主要功能

### 1. 多种DOM操作类型

- **隐藏 (Hide)**: 使用 `display: none` 隐藏元素
- **模糊 (Blur)**: 应用模糊滤镜效果
- **淡化 (Fade)**: 降低元素透明度
- **滑出 (Slide Out)**: 滑出动画效果
- **缩小 (Scale Down)**: 缩小动画效果
- **灰度 (Grayscale)**: 灰度滤镜效果
- **遮罩 (Overlay)**: 添加半透明遮罩
- **移除 (Remove)**: 完全移除元素
- **自定义CSS (Custom CSS)**: 应用自定义样式

### 2. 网站配置管理

- 添加/删除网站配置
- 启用/禁用特定网站
- 配置专注提醒消息
- 高级设置（DOM监听、延迟、重试等）

### 3. 全局设置

- 全局启用/禁用DOM配置
- 动画效果开关
- 调试模式
- 默认操作类型设置

### 4. 预设模板

- 预定义的常用操作配置
- 快速应用到新网站
- 自定义预设管理

## 使用方法

### 1. 打开配置界面

1. 点击浏览器工具栏中的扩展图标
2. 选择 "Side Panel" 或直接打开侧边栏
3. 在侧边栏中可以看到DOM配置管理界面

### 2. 添加网站配置

1. 在 "网站配置" 标签页中点击 "添加网站"
2. 输入网站域名（如：`bilibili.com`）
3. 输入网站名称（如：`Bilibili`）
4. 点击 "添加" 完成

### 3. 配置DOM操作

1. 在网站列表中选择要配置的网站
2. 在网站详情中点击 "添加操作"
3. 输入CSS选择器（如：`.advertisement`, `#nav-bar`）
4. 选择操作类型（隐藏、模糊等）
5. 添加操作描述（可选）
6. 点击 "添加" 完成

### 4. 测试配置

1. 切换到 "测试调试" 标签页
2. 输入要测试的网站URL
3. 点击 "测试" 查看匹配的配置
4. 或点击 "当前页面" 测试当前浏览的网站

## CSS选择器示例

### 常用选择器

```css
/* 类选择器 */
.advertisement
.sidebar
.popup

/* ID选择器 */
#nav-searchform
#header-ads

/* 属性选择器 */
[data-ad]
[class*="ad-"]
div[role="banner"]

/* 组合选择器 */
.sidebar .advertisement
nav > .search-box
```

### 网站特定选择器

```css
/* Bilibili */
#nav-searchform          /* 搜索框 */
.center-search__bar      /* 搜索栏 */
.video-page-game-card    /* 游戏推荐 */

/* 百度 */
#s-hotsearch-wrapper     /* 热搜区域 */
#con-ceiling-wrapper     /* 顶部导航 */

/* 知乎 */
.Topstory               /* 热门话题 */
.Card.TopstoryItem      /* 推荐内容 */
```

## 高级配置

### 动画参数

```javascript
{
  "animation": {
    "duration": 300,        // 动画持续时间(ms)
    "easing": "ease-in-out", // 缓动函数
    "delay": 0              // 动画延迟(ms)
  }
}
```

### 操作参数

```javascript
{
  "params": {
    "blurLevel": 5,         // 模糊级别(px)
    "opacity": 0.3,         // 透明度(0-1)
    "scale": 0.1,           // 缩放比例(0-1)
    "overlayColor": "rgba(0,0,0,0.5)", // 遮罩颜色
    "overlayOpacity": 0.5   // 遮罩透明度
  }
}
```

### 网站高级设置

```javascript
{
  "advanced": {
    "observeChanges": true, // 监听DOM变化
    "applyDelay": 300,      // 应用延迟(ms)
    "retryCount": 3         // 重试次数
  }
}
```

## 配置导入导出

### 导出配置

1. 在 "测试调试" 页面点击 "导出配置"
2. 配置将保存为 `dom-config.json` 文件

### 导入配置

```javascript
// 通过代码导入配置
await domConfigStorage.importConfig(configData);
```

## 故障排除

### 1. 配置不生效

- 检查全局设置是否启用
- 确认网站配置是否启用
- 验证CSS选择器是否正确
- 查看浏览器控制台是否有错误

### 2. 选择器不匹配

- 使用浏览器开发者工具检查元素
- 确认选择器语法正确
- 考虑元素是否动态加载

### 3. 动画效果异常

- 检查动画是否全局启用
- 确认动画参数设置合理
- 查看CSS冲突问题

## 最佳实践

### 1. 选择器编写

- 使用具体的类名或ID
- 避免过于宽泛的选择器
- 考虑网站结构变化

### 2. 操作类型选择

- 优先使用 "隐藏" 作为默认操作
- 对重要内容使用 "淡化" 而非 "移除"
- 谨慎使用 "移除" 操作

### 3. 性能优化

- 限制同时监听的网站数量
- 合理设置应用延迟
- 避免复杂的CSS选择器

## 技术架构

### 存储系统

- 使用Chrome Extension Storage API
- 支持实时同步更新
- 数据持久化存储

### DOM操作引擎

- 统一的DOM操作接口
- 支持动画和过渡效果
- 错误处理和降级机制

### 配置界面

- React组件化设计
- 响应式布局
- 深色/浅色主题支持

## 更新日志

### v1.0.0
- 初始版本发布
- 支持基本DOM操作类型
- 网站配置管理界面
- 预设模板系统
- 测试调试功能

## 反馈与支持

如果您在使用过程中遇到问题或有改进建议，请通过以下方式联系：

- 在扩展设置页面提交反馈
- 查看浏览器控制台错误信息
- 使用测试功能验证配置
