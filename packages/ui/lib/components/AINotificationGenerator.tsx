/**
 * AI通知生成器组件
 *
 * 注意：此组件已被弃用。AI通知生成逻辑已统一移至 Background Script 的 NotificationManager 中。
 * 保留此文件是为了向后兼容，但实际功能已不再使用。
 */
export const AINotificationGenerator = () => {
  // 这个组件不再执行任何逻辑，AI通知生成已统一到 Background Script
  return null;
};
